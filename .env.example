# SQLite (padrão, zero-config)
DATABASE_URL="file:./dev.db"

# Para Postgres, troque o provider no schema.prisma para "postgresql" e use:
# DATABASE_URL="postgresql://user:password@localhost:5432/wallet?schema=public"

# Para MySQL, troque o provider no schema.prisma para "mysql" e use:
# DATABASE_URL="mysql://user:password@localhost:3306/wallet"

PORT=3000
